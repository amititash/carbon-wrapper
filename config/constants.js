const {
    PORT,
    DATABASE,
    carbonApiKey,
    carbonApiUrl

} = process.env;

const constants = {
    PORT,
    DATABASE,
    carbonApiKey,
    carbonApiUrl
};

module.exports = {
    constants,
};