const {
    PORT,
    DATABASE,
    carbonApiKey,
    carbonApiUrl,
    browse_AI_key,
    browse_AI_Url,
    // robot_id_Extract_LinkedIn_job,

} = process.env;

const constants = {
    PORT,
    DATABASE,
    carbonApiKey,
    carbonApiUrl,
    browse_AI_key,
    browse_AI_Url,
    // robot_id_Extract_LinkedIn_job
};

module.exports = {
    constants,
};